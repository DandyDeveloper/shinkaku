# docker-compose.yml — development stack
#
# Runs the full stack in containers with the Vite dev server for hot reload.
# Copy .env.example to .env and fill in your values before starting.
#
#   docker compose up
#

services:
  backend:
    build:
      context: ./backend
    ports:
      - "${PORT:-8080}:8080"
    env_file: .env
    environment:
      # Override DB path to use the mounted volume
      DB_PATH: /data/shinkaku.db
      # Reach Ollama by container name
      OLLAMA_URL: http://ollama:11434
      # Keep Go's heap target below the container limit.
      GOMEMLIMIT: ${BACKEND_GO_MEMORY_LIMIT:-768MiB}
    volumes:
      - backend-data:/data
    mem_limit: ${BACKEND_MEMORY_LIMIT:-1g}
    depends_on:
      ollama:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      # Uses the 'dev' stage — runs the Vite dev server with hot reload
      target: dev
    ports:
      - "5173:5173"
    environment:
      # Proxy /api, /auth, /health to the backend container
      BACKEND_URL: http://backend:8080
    depends_on:
      - backend
    restart: unless-stopped

  ollama:
    container_name: ollama
    image: ollama/ollama:latest
    ports:
      - "5555:11434"
    volumes:
      - ollama-data:/root/.ollama
    # For AMD GPU passthrough, add the AMD override file documented in README.md.
    # Uncomment for NVIDIA GPU support:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "ollama list > /dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 6
      start_period: 30s
    restart: unless-stopped

networks:
  default:
    name: shinkaku-network

volumes:
  backend-data:
  ollama-data:
